import { ReactDOM } from "react"


const root = ReactDOM.createRoot(document.getElementById("area"))
function Area() {
    return 
    <div className="area">

    </div>
}

export default [Area, root]